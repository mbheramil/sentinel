apiVersion: v1
kind: Service
metadata:
  name: {{ include "sentinel.fullname" . }}-web
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sentinel.web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sentinel.web.selectorLabels" . | nindent 4 }}
