apiVersion: v1
kind: Service
metadata:
  name: {{ include "sentinel.fullname" . }}-api
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sentinel.api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sentinel.api.selectorLabels" . | nindent 4 }}
